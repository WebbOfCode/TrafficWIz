# TrafficWiz Backend Environment Variables
# Copy this file to .env and update with your MySQL credentials

# MySQL Database Configuration
DB_HOST=127.0.0.1
DB_USER=trafficwiz_user
DB_PASSWORD=StrongPass123!
DB_NAME=trafficwiz

# Flask Server Configuration
PORT=5000

# Instructions:
# 1. Install MySQL Server
# 2. Create database: CREATE DATABASE trafficwiz;
# 3. Create user: CREATE USER 'trafficwiz_user'@'localhost' IDENTIFIED BY 'StrongPass123!';
# 4. Grant permissions: GRANT ALL PRIVILEGES ON trafficwiz.* TO 'trafficwiz_user'@'localhost';
# 5. Copy this file to .env and update credentials if needed
# 6. Run start.bat or python scripts/seed_traffic.py to create tables
